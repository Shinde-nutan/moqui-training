<?xml version="1.0" encoding="UTF-8"?>
<services xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns=""
          xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/service-definition-3.xsd">

    <service verb="create" noun="ProductDetails" type="inline">
    <description>
        Create a new product
    </description>
    <in-parameters>
        <parameter name="productName" type="String"/>
        <parameter name="description" type="String"/>
        <parameter name="price" entity-name="mantle.product.ProductPrice" />
<!--        <parameter name="priceUomId" entity-name="mantle.product.ProductPrice" type="String" default="USD"/>-->
        <parameter name="priceUomId" entity-name="mantle.product.ProductPrice" type="String"/>
    </in-parameters>
    <out-parameters>
        <parameter name="productId" entity-name="mantle.product.ProductPrice"/>
    </out-parameters>
    <actions>
        <if condition="price">
            <if condition="!priceUomId">
                <then><return error="true" message="priceUomId is needed for product price"/></then>
            </if>
        </if>
        <service-call name="create#mantle.product.Product" in-map="context" out-map="context"/>
        <service-call name="create#mantle.product.ProductPrice" in-map="context" out-map="context"/>
    </actions>
    </service>

    <service verb="get" noun="ProductDetails">
        <description>
            Service to get the product details.
        </description>
        <in-parameters>
            <auto-parameters entity-name="mantle.product.Product" include="pk"/>
            <parameter name="productId" required="true"/>
        </in-parameters>
        <out-parameters>
            <parameter name="productList"/>
            <parameter name="productPriceList"/>
        </out-parameters>
        <actions>
            <entity-find entity-name="mantle.product.Product" list="productList">
                <econdition field-name="productId" value="${productId}"/>
                <select-field field-name="productName"/>
                <select-field field-name="description"/>
            </entity-find>
            <entity-find entity-name="mantle.product.ProductPrice" list="productPriceList">
                <econdition field-name="productId" value="${productId}"/>
                <select-field field-name="price"/>
                <select-field field-name="priceUomId"/>
            </entity-find>
            <if condition="!productList"><then>
                <return error="true" message="Product does not exist"/>
            </then></if>
        </actions>
    </service>
</services>




